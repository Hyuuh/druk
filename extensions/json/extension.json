{
  "id": "json",
  "name": "JSON",
  "version": "1.0.0",
  "description": "JSON highlighting and the VS Code JSON server. Also reads `bun.lock`.",
  "languages": [
    {
      "id": "json",
      "grammar": {
        "vendored": "json"
      },
      "filenames": ["bun.lock"]
    }
  ],
  "languageServers": [
    {
      "id": "json",
      "command": ["vscode-json-language-server", "--stdio"],
      "filetypes": ["json"],
      "install": {
        "kind": "npm",
        "packages": ["vscode-langservers-extracted"]
      }
    }
  ]
}
